# MediTwin AI — Backend Requirements
# ═══════════════════════════════════════════════════════════════════
# Tested: Python 3.10 / 3.11 / 3.12 / 3.13 | Windows 10/11 x64
#
# !! IMPORTANT — USE THIS COMMAND, NOT plain `pip install -r requirements.txt`:
#
#     pip install -r requirements.txt --prefer-binary
#
# The `--prefer-binary` flag makes pip download pre-built wheels
# (.whl files) instead of compiling from source. Without it, packages
# like scikit-learn try to compile using MSVC (cl.exe) which is not
# installed on most Windows machines — causing the error you saw.
#
# All packages below have confirmed Python 3.13 / Windows x64 wheels:
#   fastapi       → pure Python, no wheel needed
#   scikit-learn  → scikit_learn-1.6.0-cp313-cp313-win_amd64.whl ✓
#   numpy         → numpy-2.2.0-cp313-cp313t-win_amd64.whl        ✓
#   pandas        → pandas-2.2.3-cp313-cp313-win_amd64.whl        ✓
#   shap          → shap-0.51.0-cp313-cp313-win_amd64.whl         ✓
#     └─ numba    → numba-0.61.0-cp313-cp313-win_amd64.whl        ✓
# ═══════════════════════════════════════════════════════════════════

# ── Core API ──────────────────────────────────────────────────────
fastapi==0.115.5
uvicorn[standard]==0.32.1
python-multipart==0.0.12
pydantic==2.10.3
python-jose[cryptography]==3.3.0
python-dotenv==1.0.1

# ── AI / LLM ──────────────────────────────────────────────────────
groq==0.13.0
httpx==0.28.0

# ── ML Validation Engine ──────────────────────────────────────────
scikit-learn==1.6.0
numpy==2.2.0
pandas==2.2.3
joblib==1.4.2
shap==0.51.0

# ── Testing ───────────────────────────────────────────────────────
pytest==8.3.4
